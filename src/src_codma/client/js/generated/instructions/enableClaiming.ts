/**
 * This code was AUTOGENERATED using the codama library.
 * Please DO NOT EDIT THIS FILE, instead use visitors
 * to add features, then rerun codama to update it.
 *
 * @see https://github.com/codama-idl/codama
 */

import {
  combineCodec,
  fixDecoderSize,
  fixEncoderSize,
  getAddressEncoder,
  getBytesDecoder,
  getBytesEncoder,
  getProgramDerivedAddress,
  getStructDecoder,
  getStructEncoder,
  transformEncoder,
  type AccountMeta,
  type AccountSignerMeta,
  type Address,
  type FixedSizeCodec,
  type FixedSizeDecoder,
  type FixedSizeEncoder,
  type Instruction,
  type InstructionWithAccounts,
  type InstructionWithData,
  type ReadonlyAccount,
  type ReadonlyUint8Array,
  type TransactionSigner,
  type WritableAccount,
  type WritableSignerAccount,
} from 'gill';
import { FUNDS_CYCLE_PROGRAM_PROGRAM_ADDRESS } from '../programs';
import {
  expectAddress,
  getAccountMetaFactory,
  type ResolvedAccount,
} from '../shared';

export const ENABLE_CLAIMING_DISCRIMINATOR = new Uint8Array([
  125, 165, 100, 79, 224, 80, 88, 119,
]);

export function getEnableClaimingDiscriminatorBytes() {
  return fixEncoderSize(getBytesEncoder(), 8).encode(
    ENABLE_CLAIMING_DISCRIMINATOR
  );
}

export type EnableClaimingInstruction<
  TProgram extends string = typeof FUNDS_CYCLE_PROGRAM_PROGRAM_ADDRESS,
  TAccountSigner extends string | AccountMeta<string> = string,
  TAccountConfig extends string | AccountMeta<string> = string,
  TAccountVault extends string | AccountMeta<string> = string,
  TAccountBeneficiary extends string | AccountMeta<string> = string,
  TAccountSystemProgram extends
    | string
    | AccountMeta<string> = '11111111111111111111111111111111',
  TRemainingAccounts extends readonly AccountMeta<string>[] = [],
> = Instruction<TProgram> &
  InstructionWithData<ReadonlyUint8Array> &
  InstructionWithAccounts<
    [
      TAccountSigner extends string
        ? WritableSignerAccount<TAccountSigner> &
            AccountSignerMeta<TAccountSigner>
        : TAccountSigner,
      TAccountConfig extends string
        ? WritableAccount<TAccountConfig>
        : TAccountConfig,
      TAccountVault extends string
        ? WritableAccount<TAccountVault>
        : TAccountVault,
      TAccountBeneficiary extends string
        ? WritableAccount<TAccountBeneficiary>
        : TAccountBeneficiary,
      TAccountSystemProgram extends string
        ? ReadonlyAccount<TAccountSystemProgram>
        : TAccountSystemProgram,
      ...TRemainingAccounts,
    ]
  >;

export type EnableClaimingInstructionData = {
  discriminator: ReadonlyUint8Array;
};

export type EnableClaimingInstructionDataArgs = {};

export function getEnableClaimingInstructionDataEncoder(): FixedSizeEncoder<EnableClaimingInstructionDataArgs> {
  return transformEncoder(
    getStructEncoder([['discriminator', fixEncoderSize(getBytesEncoder(), 8)]]),
    (value) => ({ ...value, discriminator: ENABLE_CLAIMING_DISCRIMINATOR })
  );
}

export function getEnableClaimingInstructionDataDecoder(): FixedSizeDecoder<EnableClaimingInstructionData> {
  return getStructDecoder([
    ['discriminator', fixDecoderSize(getBytesDecoder(), 8)],
  ]);
}

export function getEnableClaimingInstructionDataCodec(): FixedSizeCodec<
  EnableClaimingInstructionDataArgs,
  EnableClaimingInstructionData
> {
  return combineCodec(
    getEnableClaimingInstructionDataEncoder(),
    getEnableClaimingInstructionDataDecoder()
  );
}

export type EnableClaimingAsyncInput<
  TAccountSigner extends string = string,
  TAccountConfig extends string = string,
  TAccountVault extends string = string,
  TAccountBeneficiary extends string = string,
  TAccountSystemProgram extends string = string,
> = {
  /** Admin or Beneficiary - depending on which function is called */
  signer: TransactionSigner<TAccountSigner>;
  config: Address<TAccountConfig>;
  vault?: Address<TAccountVault>;
  /** Optional - only needed for claim_collateral function */
  beneficiary?: Address<TAccountBeneficiary>;
  systemProgram?: Address<TAccountSystemProgram>;
};

export async function getEnableClaimingInstructionAsync<
  TAccountSigner extends string,
  TAccountConfig extends string,
  TAccountVault extends string,
  TAccountBeneficiary extends string,
  TAccountSystemProgram extends string,
  TProgramAddress extends Address = typeof FUNDS_CYCLE_PROGRAM_PROGRAM_ADDRESS,
>(
  input: EnableClaimingAsyncInput<
    TAccountSigner,
    TAccountConfig,
    TAccountVault,
    TAccountBeneficiary,
    TAccountSystemProgram
  >,
  config?: { programAddress?: TProgramAddress }
): Promise<
  EnableClaimingInstruction<
    TProgramAddress,
    TAccountSigner,
    TAccountConfig,
    TAccountVault,
    TAccountBeneficiary,
    TAccountSystemProgram
  >
> {
  // Program address.
  const programAddress =
    config?.programAddress ?? FUNDS_CYCLE_PROGRAM_PROGRAM_ADDRESS;

  // Original accounts.
  const originalAccounts = {
    signer: { value: input.signer ?? null, isWritable: true },
    config: { value: input.config ?? null, isWritable: true },
    vault: { value: input.vault ?? null, isWritable: true },
    beneficiary: { value: input.beneficiary ?? null, isWritable: true },
    systemProgram: { value: input.systemProgram ?? null, isWritable: false },
  };
  const accounts = originalAccounts as Record<
    keyof typeof originalAccounts,
    ResolvedAccount
  >;

  // Resolve default values.
  if (!accounts.vault.value) {
    accounts.vault.value = await getProgramDerivedAddress({
      programAddress,
      seeds: [
        getBytesEncoder().encode(new Uint8Array([118, 97, 117, 108, 116])),
        getAddressEncoder().encode(expectAddress(accounts.config.value)),
      ],
    });
  }
  if (!accounts.beneficiary.value) {
    accounts.beneficiary.value = await getProgramDerivedAddress({
      programAddress,
      seeds: [
        getBytesEncoder().encode(
          new Uint8Array([98, 101, 110, 101, 102, 105, 99, 105, 97, 114, 121])
        ),
        getAddressEncoder().encode(expectAddress(accounts.config.value)),
        getAddressEncoder().encode(expectAddress(accounts.signer.value)),
      ],
    });
  }
  if (!accounts.systemProgram.value) {
    accounts.systemProgram.value =
      '11111111111111111111111111111111' as Address<'11111111111111111111111111111111'>;
  }

  const getAccountMeta = getAccountMetaFactory(programAddress, 'programId');
  const instruction = {
    accounts: [
      getAccountMeta(accounts.signer),
      getAccountMeta(accounts.config),
      getAccountMeta(accounts.vault),
      getAccountMeta(accounts.beneficiary),
      getAccountMeta(accounts.systemProgram),
    ],
    programAddress,
    data: getEnableClaimingInstructionDataEncoder().encode({}),
  } as EnableClaimingInstruction<
    TProgramAddress,
    TAccountSigner,
    TAccountConfig,
    TAccountVault,
    TAccountBeneficiary,
    TAccountSystemProgram
  >;

  return instruction;
}

export type EnableClaimingInput<
  TAccountSigner extends string = string,
  TAccountConfig extends string = string,
  TAccountVault extends string = string,
  TAccountBeneficiary extends string = string,
  TAccountSystemProgram extends string = string,
> = {
  /** Admin or Beneficiary - depending on which function is called */
  signer: TransactionSigner<TAccountSigner>;
  config: Address<TAccountConfig>;
  vault: Address<TAccountVault>;
  /** Optional - only needed for claim_collateral function */
  beneficiary?: Address<TAccountBeneficiary>;
  systemProgram?: Address<TAccountSystemProgram>;
};

export function getEnableClaimingInstruction<
  TAccountSigner extends string,
  TAccountConfig extends string,
  TAccountVault extends string,
  TAccountBeneficiary extends string,
  TAccountSystemProgram extends string,
  TProgramAddress extends Address = typeof FUNDS_CYCLE_PROGRAM_PROGRAM_ADDRESS,
>(
  input: EnableClaimingInput<
    TAccountSigner,
    TAccountConfig,
    TAccountVault,
    TAccountBeneficiary,
    TAccountSystemProgram
  >,
  config?: { programAddress?: TProgramAddress }
): EnableClaimingInstruction<
  TProgramAddress,
  TAccountSigner,
  TAccountConfig,
  TAccountVault,
  TAccountBeneficiary,
  TAccountSystemProgram
> {
  // Program address.
  const programAddress =
    config?.programAddress ?? FUNDS_CYCLE_PROGRAM_PROGRAM_ADDRESS;

  // Original accounts.
  const originalAccounts = {
    signer: { value: input.signer ?? null, isWritable: true },
    config: { value: input.config ?? null, isWritable: true },
    vault: { value: input.vault ?? null, isWritable: true },
    beneficiary: { value: input.beneficiary ?? null, isWritable: true },
    systemProgram: { value: input.systemProgram ?? null, isWritable: false },
  };
  const accounts = originalAccounts as Record<
    keyof typeof originalAccounts,
    ResolvedAccount
  >;

  // Resolve default values.
  if (!accounts.systemProgram.value) {
    accounts.systemProgram.value =
      '11111111111111111111111111111111' as Address<'11111111111111111111111111111111'>;
  }

  const getAccountMeta = getAccountMetaFactory(programAddress, 'programId');
  const instruction = {
    accounts: [
      getAccountMeta(accounts.signer),
      getAccountMeta(accounts.config),
      getAccountMeta(accounts.vault),
      getAccountMeta(accounts.beneficiary),
      getAccountMeta(accounts.systemProgram),
    ],
    programAddress,
    data: getEnableClaimingInstructionDataEncoder().encode({}),
  } as EnableClaimingInstruction<
    TProgramAddress,
    TAccountSigner,
    TAccountConfig,
    TAccountVault,
    TAccountBeneficiary,
    TAccountSystemProgram
  >;

  return instruction;
}

export type ParsedEnableClaimingInstruction<
  TProgram extends string = typeof FUNDS_CYCLE_PROGRAM_PROGRAM_ADDRESS,
  TAccountMetas extends readonly AccountMeta[] = readonly AccountMeta[],
> = {
  programAddress: Address<TProgram>;
  accounts: {
    /** Admin or Beneficiary - depending on which function is called */
    signer: TAccountMetas[0];
    config: TAccountMetas[1];
    vault: TAccountMetas[2];
    /** Optional - only needed for claim_collateral function */
    beneficiary?: TAccountMetas[3] | undefined;
    systemProgram: TAccountMetas[4];
  };
  data: EnableClaimingInstructionData;
};

export function parseEnableClaimingInstruction<
  TProgram extends string,
  TAccountMetas extends readonly AccountMeta[],
>(
  instruction: Instruction<TProgram> &
    InstructionWithAccounts<TAccountMetas> &
    InstructionWithData<ReadonlyUint8Array>
): ParsedEnableClaimingInstruction<TProgram, TAccountMetas> {
  if (instruction.accounts.length < 5) {
    // TODO: Coded error.
    throw new Error('Not enough accounts');
  }
  let accountIndex = 0;
  const getNextAccount = () => {
    const accountMeta = (instruction.accounts as TAccountMetas)[accountIndex]!;
    accountIndex += 1;
    return accountMeta;
  };
  const getNextOptionalAccount = () => {
    const accountMeta = getNextAccount();
    return accountMeta.address === FUNDS_CYCLE_PROGRAM_PROGRAM_ADDRESS
      ? undefined
      : accountMeta;
  };
  return {
    programAddress: instruction.programAddress,
    accounts: {
      signer: getNextAccount(),
      config: getNextAccount(),
      vault: getNextAccount(),
      beneficiary: getNextOptionalAccount(),
      systemProgram: getNextAccount(),
    },
    data: getEnableClaimingInstructionDataDecoder().decode(instruction.data),
  };
}
