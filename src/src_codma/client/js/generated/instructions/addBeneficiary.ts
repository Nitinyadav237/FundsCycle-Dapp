/**
 * This code was AUTOGENERATED using the codama library.
 * Please DO NOT EDIT THIS FILE, instead use visitors
 * to add features, then rerun codama to update it.
 *
 * @see https://github.com/codama-idl/codama
 */

import {
  combineCodec,
  fixDecoderSize,
  fixEncoderSize,
  getAddressEncoder,
  getBytesDecoder,
  getBytesEncoder,
  getProgramDerivedAddress,
  getStructDecoder,
  getStructEncoder,
  transformEncoder,
  type AccountMeta,
  type AccountSignerMeta,
  type Address,
  type FixedSizeCodec,
  type FixedSizeDecoder,
  type FixedSizeEncoder,
  type Instruction,
  type InstructionWithAccounts,
  type InstructionWithData,
  type ReadonlyAccount,
  type ReadonlyUint8Array,
  type TransactionSigner,
  type WritableAccount,
  type WritableSignerAccount,
} from 'gill';
import { FUNDS_CYCLE_PROGRAM_PROGRAM_ADDRESS } from '../programs';
import {
  expectAddress,
  getAccountMetaFactory,
  type ResolvedAccount,
} from '../shared';

export const ADD_BENEFICIARY_DISCRIMINATOR = new Uint8Array([
  105, 214, 106, 141, 180, 166, 123, 238,
]);

export function getAddBeneficiaryDiscriminatorBytes() {
  return fixEncoderSize(getBytesEncoder(), 8).encode(
    ADD_BENEFICIARY_DISCRIMINATOR
  );
}

export type AddBeneficiaryInstruction<
  TProgram extends string = typeof FUNDS_CYCLE_PROGRAM_PROGRAM_ADDRESS,
  TAccountAdmin extends string | AccountMeta<string> = string,
  TAccountConfig extends string | AccountMeta<string> = string,
  TAccountWallet extends string | AccountMeta<string> = string,
  TAccountBeneficiary extends string | AccountMeta<string> = string,
  TAccountSystemProgram extends
    | string
    | AccountMeta<string> = '11111111111111111111111111111111',
  TRemainingAccounts extends readonly AccountMeta<string>[] = [],
> = Instruction<TProgram> &
  InstructionWithData<ReadonlyUint8Array> &
  InstructionWithAccounts<
    [
      TAccountAdmin extends string
        ? WritableSignerAccount<TAccountAdmin> &
            AccountSignerMeta<TAccountAdmin>
        : TAccountAdmin,
      TAccountConfig extends string
        ? WritableAccount<TAccountConfig>
        : TAccountConfig,
      TAccountWallet extends string
        ? ReadonlyAccount<TAccountWallet>
        : TAccountWallet,
      TAccountBeneficiary extends string
        ? WritableAccount<TAccountBeneficiary>
        : TAccountBeneficiary,
      TAccountSystemProgram extends string
        ? ReadonlyAccount<TAccountSystemProgram>
        : TAccountSystemProgram,
      ...TRemainingAccounts,
    ]
  >;

export type AddBeneficiaryInstructionData = {
  discriminator: ReadonlyUint8Array;
};

export type AddBeneficiaryInstructionDataArgs = {};

export function getAddBeneficiaryInstructionDataEncoder(): FixedSizeEncoder<AddBeneficiaryInstructionDataArgs> {
  return transformEncoder(
    getStructEncoder([['discriminator', fixEncoderSize(getBytesEncoder(), 8)]]),
    (value) => ({ ...value, discriminator: ADD_BENEFICIARY_DISCRIMINATOR })
  );
}

export function getAddBeneficiaryInstructionDataDecoder(): FixedSizeDecoder<AddBeneficiaryInstructionData> {
  return getStructDecoder([
    ['discriminator', fixDecoderSize(getBytesDecoder(), 8)],
  ]);
}

export function getAddBeneficiaryInstructionDataCodec(): FixedSizeCodec<
  AddBeneficiaryInstructionDataArgs,
  AddBeneficiaryInstructionData
> {
  return combineCodec(
    getAddBeneficiaryInstructionDataEncoder(),
    getAddBeneficiaryInstructionDataDecoder()
  );
}

export type AddBeneficiaryAsyncInput<
  TAccountAdmin extends string = string,
  TAccountConfig extends string = string,
  TAccountWallet extends string = string,
  TAccountBeneficiary extends string = string,
  TAccountSystemProgram extends string = string,
> = {
  admin: TransactionSigner<TAccountAdmin>;
  config: Address<TAccountConfig>;
  /**
   * This is the wallet we are adding as beneficiary
   * Can be the same as `admin` or different — usually a different user.
   */
  wallet: Address<TAccountWallet>;
  beneficiary?: Address<TAccountBeneficiary>;
  systemProgram?: Address<TAccountSystemProgram>;
};

export async function getAddBeneficiaryInstructionAsync<
  TAccountAdmin extends string,
  TAccountConfig extends string,
  TAccountWallet extends string,
  TAccountBeneficiary extends string,
  TAccountSystemProgram extends string,
  TProgramAddress extends Address = typeof FUNDS_CYCLE_PROGRAM_PROGRAM_ADDRESS,
>(
  input: AddBeneficiaryAsyncInput<
    TAccountAdmin,
    TAccountConfig,
    TAccountWallet,
    TAccountBeneficiary,
    TAccountSystemProgram
  >,
  config?: { programAddress?: TProgramAddress }
): Promise<
  AddBeneficiaryInstruction<
    TProgramAddress,
    TAccountAdmin,
    TAccountConfig,
    TAccountWallet,
    TAccountBeneficiary,
    TAccountSystemProgram
  >
> {
  // Program address.
  const programAddress =
    config?.programAddress ?? FUNDS_CYCLE_PROGRAM_PROGRAM_ADDRESS;

  // Original accounts.
  const originalAccounts = {
    admin: { value: input.admin ?? null, isWritable: true },
    config: { value: input.config ?? null, isWritable: true },
    wallet: { value: input.wallet ?? null, isWritable: false },
    beneficiary: { value: input.beneficiary ?? null, isWritable: true },
    systemProgram: { value: input.systemProgram ?? null, isWritable: false },
  };
  const accounts = originalAccounts as Record<
    keyof typeof originalAccounts,
    ResolvedAccount
  >;

  // Resolve default values.
  if (!accounts.beneficiary.value) {
    accounts.beneficiary.value = await getProgramDerivedAddress({
      programAddress,
      seeds: [
        getBytesEncoder().encode(
          new Uint8Array([98, 101, 110, 101, 102, 105, 99, 105, 97, 114, 121])
        ),
        getAddressEncoder().encode(expectAddress(accounts.config.value)),
        getAddressEncoder().encode(expectAddress(accounts.wallet.value)),
      ],
    });
  }
  if (!accounts.systemProgram.value) {
    accounts.systemProgram.value =
      '11111111111111111111111111111111' as Address<'11111111111111111111111111111111'>;
  }

  const getAccountMeta = getAccountMetaFactory(programAddress, 'programId');
  const instruction = {
    accounts: [
      getAccountMeta(accounts.admin),
      getAccountMeta(accounts.config),
      getAccountMeta(accounts.wallet),
      getAccountMeta(accounts.beneficiary),
      getAccountMeta(accounts.systemProgram),
    ],
    programAddress,
    data: getAddBeneficiaryInstructionDataEncoder().encode({}),
  } as AddBeneficiaryInstruction<
    TProgramAddress,
    TAccountAdmin,
    TAccountConfig,
    TAccountWallet,
    TAccountBeneficiary,
    TAccountSystemProgram
  >;

  return instruction;
}

export type AddBeneficiaryInput<
  TAccountAdmin extends string = string,
  TAccountConfig extends string = string,
  TAccountWallet extends string = string,
  TAccountBeneficiary extends string = string,
  TAccountSystemProgram extends string = string,
> = {
  admin: TransactionSigner<TAccountAdmin>;
  config: Address<TAccountConfig>;
  /**
   * This is the wallet we are adding as beneficiary
   * Can be the same as `admin` or different — usually a different user.
   */
  wallet: Address<TAccountWallet>;
  beneficiary: Address<TAccountBeneficiary>;
  systemProgram?: Address<TAccountSystemProgram>;
};

export function getAddBeneficiaryInstruction<
  TAccountAdmin extends string,
  TAccountConfig extends string,
  TAccountWallet extends string,
  TAccountBeneficiary extends string,
  TAccountSystemProgram extends string,
  TProgramAddress extends Address = typeof FUNDS_CYCLE_PROGRAM_PROGRAM_ADDRESS,
>(
  input: AddBeneficiaryInput<
    TAccountAdmin,
    TAccountConfig,
    TAccountWallet,
    TAccountBeneficiary,
    TAccountSystemProgram
  >,
  config?: { programAddress?: TProgramAddress }
): AddBeneficiaryInstruction<
  TProgramAddress,
  TAccountAdmin,
  TAccountConfig,
  TAccountWallet,
  TAccountBeneficiary,
  TAccountSystemProgram
> {
  // Program address.
  const programAddress =
    config?.programAddress ?? FUNDS_CYCLE_PROGRAM_PROGRAM_ADDRESS;

  // Original accounts.
  const originalAccounts = {
    admin: { value: input.admin ?? null, isWritable: true },
    config: { value: input.config ?? null, isWritable: true },
    wallet: { value: input.wallet ?? null, isWritable: false },
    beneficiary: { value: input.beneficiary ?? null, isWritable: true },
    systemProgram: { value: input.systemProgram ?? null, isWritable: false },
  };
  const accounts = originalAccounts as Record<
    keyof typeof originalAccounts,
    ResolvedAccount
  >;

  // Resolve default values.
  if (!accounts.systemProgram.value) {
    accounts.systemProgram.value =
      '11111111111111111111111111111111' as Address<'11111111111111111111111111111111'>;
  }

  const getAccountMeta = getAccountMetaFactory(programAddress, 'programId');
  const instruction = {
    accounts: [
      getAccountMeta(accounts.admin),
      getAccountMeta(accounts.config),
      getAccountMeta(accounts.wallet),
      getAccountMeta(accounts.beneficiary),
      getAccountMeta(accounts.systemProgram),
    ],
    programAddress,
    data: getAddBeneficiaryInstructionDataEncoder().encode({}),
  } as AddBeneficiaryInstruction<
    TProgramAddress,
    TAccountAdmin,
    TAccountConfig,
    TAccountWallet,
    TAccountBeneficiary,
    TAccountSystemProgram
  >;

  return instruction;
}

export type ParsedAddBeneficiaryInstruction<
  TProgram extends string = typeof FUNDS_CYCLE_PROGRAM_PROGRAM_ADDRESS,
  TAccountMetas extends readonly AccountMeta[] = readonly AccountMeta[],
> = {
  programAddress: Address<TProgram>;
  accounts: {
    admin: TAccountMetas[0];
    config: TAccountMetas[1];
    /**
     * This is the wallet we are adding as beneficiary
     * Can be the same as `admin` or different — usually a different user.
     */

    wallet: TAccountMetas[2];
    beneficiary: TAccountMetas[3];
    systemProgram: TAccountMetas[4];
  };
  data: AddBeneficiaryInstructionData;
};

export function parseAddBeneficiaryInstruction<
  TProgram extends string,
  TAccountMetas extends readonly AccountMeta[],
>(
  instruction: Instruction<TProgram> &
    InstructionWithAccounts<TAccountMetas> &
    InstructionWithData<ReadonlyUint8Array>
): ParsedAddBeneficiaryInstruction<TProgram, TAccountMetas> {
  if (instruction.accounts.length < 5) {
    // TODO: Coded error.
    throw new Error('Not enough accounts');
  }
  let accountIndex = 0;
  const getNextAccount = () => {
    const accountMeta = (instruction.accounts as TAccountMetas)[accountIndex]!;
    accountIndex += 1;
    return accountMeta;
  };
  return {
    programAddress: instruction.programAddress,
    accounts: {
      admin: getNextAccount(),
      config: getNextAccount(),
      wallet: getNextAccount(),
      beneficiary: getNextAccount(),
      systemProgram: getNextAccount(),
    },
    data: getAddBeneficiaryInstructionDataDecoder().decode(instruction.data),
  };
}
