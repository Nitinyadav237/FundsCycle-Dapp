/**
 * This code was AUTOGENERATED using the codama library.
 * Please DO NOT EDIT THIS FILE, instead use visitors
 * to add features, then rerun codama to update it.
 *
 * @see https://github.com/codama-idl/codama
 */

import {
  combineCodec,
  fixDecoderSize,
  fixEncoderSize,
  getAddressEncoder,
  getBytesDecoder,
  getBytesEncoder,
  getProgramDerivedAddress,
  getStructDecoder,
  getStructEncoder,
  transformEncoder,
  type AccountMeta,
  type AccountSignerMeta,
  type Address,
  type FixedSizeCodec,
  type FixedSizeDecoder,
  type FixedSizeEncoder,
  type Instruction,
  type InstructionWithAccounts,
  type InstructionWithData,
  type ReadonlyAccount,
  type ReadonlyUint8Array,
  type TransactionSigner,
  type WritableAccount,
  type WritableSignerAccount,
} from 'gill';
import { FUNDS_CYCLE_PROGRAM_PROGRAM_ADDRESS } from '../programs';
import {
  expectAddress,
  getAccountMetaFactory,
  type ResolvedAccount,
} from '../shared';

export const DEPOSIT_MONTHLY_DISCRIMINATOR = new Uint8Array([
  252, 123, 80, 64, 66, 80, 61, 199,
]);

export function getDepositMonthlyDiscriminatorBytes() {
  return fixEncoderSize(getBytesEncoder(), 8).encode(
    DEPOSIT_MONTHLY_DISCRIMINATOR
  );
}

export type DepositMonthlyInstruction<
  TProgram extends string = typeof FUNDS_CYCLE_PROGRAM_PROGRAM_ADDRESS,
  TAccountWallet extends string | AccountMeta<string> = string,
  TAccountConfig extends string | AccountMeta<string> = string,
  TAccountBeneficiary extends string | AccountMeta<string> = string,
  TAccountVault extends string | AccountMeta<string> = string,
  TAccountSystemProgram extends
    | string
    | AccountMeta<string> = '11111111111111111111111111111111',
  TRemainingAccounts extends readonly AccountMeta<string>[] = [],
> = Instruction<TProgram> &
  InstructionWithData<ReadonlyUint8Array> &
  InstructionWithAccounts<
    [
      TAccountWallet extends string
        ? WritableSignerAccount<TAccountWallet> &
            AccountSignerMeta<TAccountWallet>
        : TAccountWallet,
      TAccountConfig extends string
        ? ReadonlyAccount<TAccountConfig>
        : TAccountConfig,
      TAccountBeneficiary extends string
        ? WritableAccount<TAccountBeneficiary>
        : TAccountBeneficiary,
      TAccountVault extends string
        ? WritableAccount<TAccountVault>
        : TAccountVault,
      TAccountSystemProgram extends string
        ? ReadonlyAccount<TAccountSystemProgram>
        : TAccountSystemProgram,
      ...TRemainingAccounts,
    ]
  >;

export type DepositMonthlyInstructionData = {
  discriminator: ReadonlyUint8Array;
};

export type DepositMonthlyInstructionDataArgs = {};

export function getDepositMonthlyInstructionDataEncoder(): FixedSizeEncoder<DepositMonthlyInstructionDataArgs> {
  return transformEncoder(
    getStructEncoder([['discriminator', fixEncoderSize(getBytesEncoder(), 8)]]),
    (value) => ({ ...value, discriminator: DEPOSIT_MONTHLY_DISCRIMINATOR })
  );
}

export function getDepositMonthlyInstructionDataDecoder(): FixedSizeDecoder<DepositMonthlyInstructionData> {
  return getStructDecoder([
    ['discriminator', fixDecoderSize(getBytesDecoder(), 8)],
  ]);
}

export function getDepositMonthlyInstructionDataCodec(): FixedSizeCodec<
  DepositMonthlyInstructionDataArgs,
  DepositMonthlyInstructionData
> {
  return combineCodec(
    getDepositMonthlyInstructionDataEncoder(),
    getDepositMonthlyInstructionDataDecoder()
  );
}

export type DepositMonthlyAsyncInput<
  TAccountWallet extends string = string,
  TAccountConfig extends string = string,
  TAccountBeneficiary extends string = string,
  TAccountVault extends string = string,
  TAccountSystemProgram extends string = string,
> = {
  wallet: TransactionSigner<TAccountWallet>;
  config: Address<TAccountConfig>;
  beneficiary?: Address<TAccountBeneficiary>;
  vault?: Address<TAccountVault>;
  systemProgram?: Address<TAccountSystemProgram>;
};

export async function getDepositMonthlyInstructionAsync<
  TAccountWallet extends string,
  TAccountConfig extends string,
  TAccountBeneficiary extends string,
  TAccountVault extends string,
  TAccountSystemProgram extends string,
  TProgramAddress extends Address = typeof FUNDS_CYCLE_PROGRAM_PROGRAM_ADDRESS,
>(
  input: DepositMonthlyAsyncInput<
    TAccountWallet,
    TAccountConfig,
    TAccountBeneficiary,
    TAccountVault,
    TAccountSystemProgram
  >,
  config?: { programAddress?: TProgramAddress }
): Promise<
  DepositMonthlyInstruction<
    TProgramAddress,
    TAccountWallet,
    TAccountConfig,
    TAccountBeneficiary,
    TAccountVault,
    TAccountSystemProgram
  >
> {
  // Program address.
  const programAddress =
    config?.programAddress ?? FUNDS_CYCLE_PROGRAM_PROGRAM_ADDRESS;

  // Original accounts.
  const originalAccounts = {
    wallet: { value: input.wallet ?? null, isWritable: true },
    config: { value: input.config ?? null, isWritable: false },
    beneficiary: { value: input.beneficiary ?? null, isWritable: true },
    vault: { value: input.vault ?? null, isWritable: true },
    systemProgram: { value: input.systemProgram ?? null, isWritable: false },
  };
  const accounts = originalAccounts as Record<
    keyof typeof originalAccounts,
    ResolvedAccount
  >;

  // Resolve default values.
  if (!accounts.beneficiary.value) {
    accounts.beneficiary.value = await getProgramDerivedAddress({
      programAddress,
      seeds: [
        getBytesEncoder().encode(
          new Uint8Array([98, 101, 110, 101, 102, 105, 99, 105, 97, 114, 121])
        ),
        getAddressEncoder().encode(expectAddress(accounts.config.value)),
        getAddressEncoder().encode(expectAddress(accounts.wallet.value)),
      ],
    });
  }
  if (!accounts.vault.value) {
    accounts.vault.value = await getProgramDerivedAddress({
      programAddress,
      seeds: [
        getBytesEncoder().encode(new Uint8Array([118, 97, 117, 108, 116])),
        getAddressEncoder().encode(expectAddress(accounts.config.value)),
      ],
    });
  }
  if (!accounts.systemProgram.value) {
    accounts.systemProgram.value =
      '11111111111111111111111111111111' as Address<'11111111111111111111111111111111'>;
  }

  const getAccountMeta = getAccountMetaFactory(programAddress, 'programId');
  const instruction = {
    accounts: [
      getAccountMeta(accounts.wallet),
      getAccountMeta(accounts.config),
      getAccountMeta(accounts.beneficiary),
      getAccountMeta(accounts.vault),
      getAccountMeta(accounts.systemProgram),
    ],
    programAddress,
    data: getDepositMonthlyInstructionDataEncoder().encode({}),
  } as DepositMonthlyInstruction<
    TProgramAddress,
    TAccountWallet,
    TAccountConfig,
    TAccountBeneficiary,
    TAccountVault,
    TAccountSystemProgram
  >;

  return instruction;
}

export type DepositMonthlyInput<
  TAccountWallet extends string = string,
  TAccountConfig extends string = string,
  TAccountBeneficiary extends string = string,
  TAccountVault extends string = string,
  TAccountSystemProgram extends string = string,
> = {
  wallet: TransactionSigner<TAccountWallet>;
  config: Address<TAccountConfig>;
  beneficiary: Address<TAccountBeneficiary>;
  vault: Address<TAccountVault>;
  systemProgram?: Address<TAccountSystemProgram>;
};

export function getDepositMonthlyInstruction<
  TAccountWallet extends string,
  TAccountConfig extends string,
  TAccountBeneficiary extends string,
  TAccountVault extends string,
  TAccountSystemProgram extends string,
  TProgramAddress extends Address = typeof FUNDS_CYCLE_PROGRAM_PROGRAM_ADDRESS,
>(
  input: DepositMonthlyInput<
    TAccountWallet,
    TAccountConfig,
    TAccountBeneficiary,
    TAccountVault,
    TAccountSystemProgram
  >,
  config?: { programAddress?: TProgramAddress }
): DepositMonthlyInstruction<
  TProgramAddress,
  TAccountWallet,
  TAccountConfig,
  TAccountBeneficiary,
  TAccountVault,
  TAccountSystemProgram
> {
  // Program address.
  const programAddress =
    config?.programAddress ?? FUNDS_CYCLE_PROGRAM_PROGRAM_ADDRESS;

  // Original accounts.
  const originalAccounts = {
    wallet: { value: input.wallet ?? null, isWritable: true },
    config: { value: input.config ?? null, isWritable: false },
    beneficiary: { value: input.beneficiary ?? null, isWritable: true },
    vault: { value: input.vault ?? null, isWritable: true },
    systemProgram: { value: input.systemProgram ?? null, isWritable: false },
  };
  const accounts = originalAccounts as Record<
    keyof typeof originalAccounts,
    ResolvedAccount
  >;

  // Resolve default values.
  if (!accounts.systemProgram.value) {
    accounts.systemProgram.value =
      '11111111111111111111111111111111' as Address<'11111111111111111111111111111111'>;
  }

  const getAccountMeta = getAccountMetaFactory(programAddress, 'programId');
  const instruction = {
    accounts: [
      getAccountMeta(accounts.wallet),
      getAccountMeta(accounts.config),
      getAccountMeta(accounts.beneficiary),
      getAccountMeta(accounts.vault),
      getAccountMeta(accounts.systemProgram),
    ],
    programAddress,
    data: getDepositMonthlyInstructionDataEncoder().encode({}),
  } as DepositMonthlyInstruction<
    TProgramAddress,
    TAccountWallet,
    TAccountConfig,
    TAccountBeneficiary,
    TAccountVault,
    TAccountSystemProgram
  >;

  return instruction;
}

export type ParsedDepositMonthlyInstruction<
  TProgram extends string = typeof FUNDS_CYCLE_PROGRAM_PROGRAM_ADDRESS,
  TAccountMetas extends readonly AccountMeta[] = readonly AccountMeta[],
> = {
  programAddress: Address<TProgram>;
  accounts: {
    wallet: TAccountMetas[0];
    config: TAccountMetas[1];
    beneficiary: TAccountMetas[2];
    vault: TAccountMetas[3];
    systemProgram: TAccountMetas[4];
  };
  data: DepositMonthlyInstructionData;
};

export function parseDepositMonthlyInstruction<
  TProgram extends string,
  TAccountMetas extends readonly AccountMeta[],
>(
  instruction: Instruction<TProgram> &
    InstructionWithAccounts<TAccountMetas> &
    InstructionWithData<ReadonlyUint8Array>
): ParsedDepositMonthlyInstruction<TProgram, TAccountMetas> {
  if (instruction.accounts.length < 5) {
    // TODO: Coded error.
    throw new Error('Not enough accounts');
  }
  let accountIndex = 0;
  const getNextAccount = () => {
    const accountMeta = (instruction.accounts as TAccountMetas)[accountIndex]!;
    accountIndex += 1;
    return accountMeta;
  };
  return {
    programAddress: instruction.programAddress,
    accounts: {
      wallet: getNextAccount(),
      config: getNextAccount(),
      beneficiary: getNextAccount(),
      vault: getNextAccount(),
      systemProgram: getNextAccount(),
    },
    data: getDepositMonthlyInstructionDataDecoder().decode(instruction.data),
  };
}
